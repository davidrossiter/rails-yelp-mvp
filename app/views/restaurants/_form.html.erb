<%= simple_form_for(@restaurant) do |f| %>
  <%= f.input :name %>
  <%= f.input :address %>
  <%= f.input :phone_number %>
  <%= f.input :category, selected: "chinese", collection: %w(chinese italian japanese belgian french) %>

  <%= f.button :submit %>
<% end %>
